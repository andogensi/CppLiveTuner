@PACKAGE_INIT@

include(CMakeFindDependencyMacro)
find_dependency(Threads)

include("${CMAKE_CURRENT_LIST_DIR}/LiveTunerTargets.cmake")
if(NOT TARGET LiveTuner::LiveTuner)
    add_library(LiveTuner::LiveTuner ALIAS LiveTuner::header_only)
endif()

check_required_components(LiveTuner)
